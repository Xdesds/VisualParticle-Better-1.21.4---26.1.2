allprojects {
    buildscript {
        repositories {
            maven { url = uri('C:/tmp/fabric-maven') }
        }
    }

    repositories {
        maven { url = uri('C:/tmp/fabric-maven') }
    }
}
